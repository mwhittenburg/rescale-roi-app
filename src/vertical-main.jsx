import React from "react";
import ReactDOM from "react-dom/client";
import VerticalApp from "./vertical-app";
import "./vertical-styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VerticalApp />
  </React.StrictMode>,
);
